{
  "name": "inspired-tech-quiz",
  "version": "1.0.0",
  "description": "Real-time multiplayer quiz app",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"npm run start\" \"cd frontend && npm start\"",
    "install-all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}

